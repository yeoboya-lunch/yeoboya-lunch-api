logging:
  config: classpath:log/logback-local.xml

spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/lunch
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: 'true'
        show_sql: 'true'
        generate_statistics: true
        default_batch_fetch_size: 100
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
    defer-datasource-initialization: true
  sql:
    init:
      mode: always

  redis:
    master:
      host: ENC(NWu1FTq9ETA2Z8oOWAbCoynMM1pG7LLo)
      port: 6379
    slaves:
      - host: ENC(NWu1FTq9ETA2Z8oOWAbCoynMM1pG7LLo)
        port: 6379
    password: ENC(ide1Ymb62UfD0C14HGaEXSOF640A/Xwy)
    database: 0


